Get the status of all git repositories in a given directory.

- Install dependencies with `poetry install`.
- Run program with `poetry run python main.py <directory>`.
- Run tests with `poetry run pytest`.
- Build a standalone executable with `poetry run python -m nuitka main.py --standalone --onefile --follow-imports --output-filename=git_status_in_repos.exe`.
